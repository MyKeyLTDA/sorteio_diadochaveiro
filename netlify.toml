[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# rota amigavel para as functions: /api/<nome>
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
